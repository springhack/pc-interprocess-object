{
  "name": "pc-interprocess-object",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "node-gyp clean && node-gyp configure && node-gyp build"
  },
  "dependencies": {
    "bindings": "^1.3.0",
    "nan": "^2.11.1"
  }
}
